PR #162: ci: enable merge queue builds
URL: https://github.com/tenstorrent/tt-llk/pull/162

File: .github/workflows/on-pr.yml
@@ -4,6 +4,7 @@ on:
   pull_request:
     types: [opened, synchronize, reopened, ready_for_review]
     branches: ["main"]
+  merge_group:
 
 concurrency:
   # Use github.run_id on main branch (or any protected branch)

