PR #231: test: try testing sinh and cosh
URL: https://github.com/tenstorrent/tt-llk/pull/231

File: tests/firmware/riscv/common/llk_sfpu_types.h
@@ -91,4 +91,6 @@ enum class SfpuType
     fill,
     prelu,
     reshuffle_rows,
+    cosh,
+    sinh,
 };

File: tests/helpers/include/params.h
@@ -224,6 +224,15 @@ constexpr auto SFPU_OPERATION = SfpuType::sine;
 #ifdef SFPU_OP_COSINE
 constexpr auto SFPU_OPERATION = SfpuType::cosine;
 #endif
+#ifdef SFPU_OP_SINH
+constexpr auto SFPU_OPERATION = SfpuType::sinh;
+#endif
+#ifdef SFPU_OP_COSH
+constexpr auto SFPU_OPERATION = SfpuType::cosh;
+#endif
+#ifdef SFPU_OP_TANH
+constexpr auto SFPU_OPERATION = SfpuType::tanh;
+#endif
 #ifdef SFPU_OP_ABS
 constexpr auto SFPU_OPERATION = SfpuType::abs;
 #endif

File: tests/python_tests/helpers/format_arg_mapping.py
@@ -89,6 +89,9 @@ class MathOperation(Enum):
     Log = "SFPU_OP_LOG"
     Sin = "SFPU_OP_SINE"
     Cos = "SFPU_OP_COSINE"
+    Sinh = "SFPU_OP_SINH"
+    Cosh = "SFPU_OP_COSH"
+    Tanh = "SFPU_OP_TANH"
     Reciprocal = "SFPU_OP_RECIPROCAL"
     ReduceColumn = "REDUCE_COL_OPERATION"
     ReduceRow = "REDUCE_ROW_OPERATION"

File: tests/python_tests/test_eltwise_unary_sfpu.py
@@ -40,11 +40,14 @@ def generate_golden(operation, operand1, data_format):
     ops = {
         MathOperation.Abs: lambda x: abs(x),
         MathOperation.Cos: lambda x: math.cos(x),
+        MathOperation.Sinh: lambda x: math.sinh(x),
+        MathOperation.Cosh: lambda x: math.cosh(x),
         MathOperation.Log: lambda x: math.log(x) if x != 0 else float("nan"),
         MathOperation.Reciprocal: lambda x: 1 / x if x != 0 else float("nan"),
         MathOperation.Sin: lambda x: math.sin(x),
         MathOperation.Sqrt: lambda x: math.sqrt(x),
         MathOperation.Square: lambda x: x * x,
+        MathOperation.Tanh: lambda x: math.tanh(x),
     }
     if operation not in ops:
         raise ValueError("Unsupported operation!")
@@ -78,13 +81,16 @@ def generate_golden(operation, operand1, data_format):
     dest_acc=[DestAccumulation.No, DestAccumulation.Yes],
     approx_mode=[ApproximationMode.No, ApproximationMode.Yes],
     mathop=[
-        MathOperation.Abs,
-        MathOperation.Cos,
-        MathOperation.Log,
-        MathOperation.Reciprocal,
-        MathOperation.Sin,
-        MathOperation.Sqrt,
-        MathOperation.Square,
+        # MathOperation.Abs,
+        # MathOperation.Cos,
+        MathOperation.Sinh,
+        MathOperation.Cosh,
+        # MathOperation.Tanh,
+        # MathOperation.Log,
+        # MathOperation.Reciprocal,
+        # MathOperation.Sin,
+        # MathOperation.Sqrt,
+        # MathOperation.Square,
     ],
 )
 param_ids = generate_param_ids(all_params)

File: tests/sources/eltwise_unary_sfpu_test.cpp
@@ -52,6 +52,9 @@ void call_sfpu_operation(SfpuType operation)
         case SfpuType::abs:
             ckernel::sfpu::_calculate_abs_<APPROX_MODE, 10>(10);
             break;
+        case SfpuType::cosh:
+            ckernel::sfpu::_calculate_cosh_<APPROX_MODE, 10>();
+            break;
         case SfpuType::cosine:
             ckernel::sfpu::_calculate_cosine_<APPROX_MODE, 10>();
             break;
@@ -63,6 +66,9 @@ void call_sfpu_operation(SfpuType operation)
             ckernel::sfpu::_init_reciprocal_<APPROX_MODE>();
             ckernel::sfpu::_calculate_reciprocal_<APPROX_MODE, 10, is_fp32_dest_acc_en>(10);
             break;
+        case SfpuType::sinh:
+            ckernel::sfpu::_calculate_sinh_<APPROX_MODE, 10>();
+            break;
         case SfpuType::sine:
             ckernel::sfpu::_calculate_sine_<APPROX_MODE, 10>();
             break;
@@ -73,6 +79,10 @@ void call_sfpu_operation(SfpuType operation)
         case SfpuType::square:
             ckernel::sfpu::_calculate_square_<APPROX_MODE, 10>(10);
             break;
+        case SfpuType::tanh:
+            ckernel::sfpu::_init_tanh_<APPROX_MODE>();
+            ckernel::sfpu::_calculate_tanh_<APPROX_MODE, 10>(10);
+            break;
         default:
             return;
     }

File: tt_llk_blackhole/common/inc/sfpu/ckernel_sfpu_trigonometry.h
@@ -192,5 +192,31 @@ inline void _calculate_cosine_()
     }
 }
 
+template <bool APPROXIMATION_MODE, int ITERATIONS>
+inline void _calculate_cosh_()
+{
+    // SFPU microcode
+    for (int d = 0; d < ITERATIONS; d++)
+    {
+        sfpi::vFloat v      = sfpi::dst_reg[0];
+        sfpi::vFloat result = (_sfpu_exp_(v) + _sfpu_exp_(-v)) * 0.5f;
+        sfpi::dst_reg[0]    = result;
+        sfpi::dst_reg++;
+    }
+}
+
+template <bool APPROXIMATION_MODE, int ITERATIONS>
+inline void _calculate_sinh_()
+{
+    // SFPU microcode
+    for (int d = 0; d < ITERATIONS; d++)
+    {
+        sfpi::vFloat v      = sfpi::dst_reg[0];
+        sfpi::vFloat result = (_sfpu_exp_(v) - _sfpu_exp_(-v)) * 0.5f;
+        sfpi::dst_reg[0]    = result;
+        sfpi::dst_reg++;
+    }
+}
+
 } // namespace sfpu
 } // namespace ckernel

File: tt_llk_wormhole_b0/common/inc/sfpu/ckernel_sfpu_trigonometry.h
@@ -187,5 +187,31 @@ inline void _calculate_cosine_()
     }
 }
 
+template <bool APPROXIMATION_MODE, int ITERATIONS>
+inline void _calculate_cosh_()
+{
+    // SFPU microcode
+    for (int d = 0; d < ITERATIONS; d++)
+    {
+        sfpi::vFloat v      = sfpi::dst_reg[0];
+        sfpi::vFloat result = (_sfpu_exp_(v) + _sfpu_exp_(-v)) * 0.5f;
+        sfpi::dst_reg[0]    = result;
+        sfpi::dst_reg++;
+    }
+}
+
+template <bool APPROXIMATION_MODE, int ITERATIONS>
+inline void _calculate_sinh_()
+{
+    // SFPU microcode
+    for (int d = 0; d < ITERATIONS; d++)
+    {
+        sfpi::vFloat v      = sfpi::dst_reg[0];
+        sfpi::vFloat result = (_sfpu_exp_(v) - _sfpu_exp_(-v)) * 0.5f;
+        sfpi::dst_reg[0]    = result;
+        sfpi::dst_reg++;
+    }
+}
+
 } // namespace sfpu
 } // namespace ckernel

