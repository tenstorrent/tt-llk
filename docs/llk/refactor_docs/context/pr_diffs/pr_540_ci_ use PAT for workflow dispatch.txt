PR #540: ci: use PAT for workflow dispatch
URL: https://github.com/tenstorrent/tt-llk/pull/540

File: .github/workflows/trigger-tt-metal-bump.yml
@@ -16,7 +16,7 @@ jobs:
       - name: Send dispatch event to tt-metal
         uses: actions/github-script@v7
         with:
-          github-token: ${{ secrets.GITHUB_TOKEN }}
+          github-token: ${{ secrets.TT_METAL_PAT }}
           retries: 3
           retry-exempt-status-codes: 400,401,403,404,422
           script: |

